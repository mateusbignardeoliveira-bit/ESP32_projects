#ifndef CONTROLE_LINHA_H
#define CONTROLE_LINHA_H

#include <Arduino.h>

#include "../sensores/LinhaAnalise.h"
#include "PIDLinha.h"
#include "../Hardware/MotorControlador.h"


class ControleLinha
{

private:

    // ========================================================
    // Referências
    // ========================================================

    PIDLinha& pid;

    MotorControlador& motores;


    // ========================================================
    // Estado
    // ========================================================

    bool ativo;


    // ========================================================
    // Velocidade
    // ========================================================

    int velocidadeBase;

    int velocidadeMaxima;


    // ========================================================
    // Tempo
    // ========================================================

    unsigned long ultimoTempo;


    // ========================================================
    // Limita velocidade
    // ========================================================

    int limitarVelocidade(int velocidade);


public:

    // ========================================================
    // CONSTRUTOR
    // ========================================================

    ControleLinha(
        PIDLinha& controladorPID,
        MotorControlador& controladorMotores
    );


    // ========================================================
    // INICIALIZAÇÃO
    // ========================================================

    void begin();


    // ========================================================
    // INICIA SEGUIMENTO
    // ========================================================

    void start();


    // ========================================================
    // ATUALIZA CONTROLE
    // ========================================================

    void update(
        const LinhaData& linha
    );


    // ========================================================
    // PARA SEGUIMENTO
    // ========================================================

    void stop();


    // ========================================================
    // RESET PID
    // ========================================================

    void reset();


    // ========================================================
    // ESTADO
    // ========================================================

    bool ativoAgora() const;


    // ========================================================
    // VELOCIDADE BASE
    // ========================================================

    void setVelocidadeBase(
        int velocidade
    );


    int getVelocidadeBase() const;


    // ========================================================
    // VELOCIDADE MÁXIMA
    // ========================================================

    void setVelocidadeMaxima(
        int velocidade
    );

};

#endif