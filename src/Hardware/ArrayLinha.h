#ifndef ARRAYLINHA_H
#define ARRAYLINHA_H

#include <Arduino.h>


// ============================================================
// Dados fornecidos pelo array Yahboom
// ============================================================

struct ArrayData
{

    // ========================================================
    // Valores ANALÓGICOS
    // ========================================================

    int s1;
    int s2;
    int s3;
    int s4;

    int s5;
    int s6;
    int s7;
    int s8;


    // ========================================================
    // Valores DIGITAIS
    // ========================================================

    uint8_t d1;
    uint8_t d2;
    uint8_t d3;
    uint8_t d4;

    uint8_t d5;
    uint8_t d6;
    uint8_t d7;
    uint8_t d8;

};


// ============================================================
// Classe ArrayLinha
// ============================================================

class ArrayLinha
{

private:

    // ========================================================
    // Serial do sensor
    // ========================================================

    HardwareSerial& serial;


    int rxPin;

    int txPin;

    int baud;


    // ========================================================
    // Buffer de recepção
    // ========================================================

    char buffer[160];

    int indice;


    // ========================================================
    // Dados atuais
    // ========================================================

    ArrayData dados;


    // ========================================================
    // Processamento dos pacotes
    // ========================================================

    void processarPacote();


    void processarAnalogico();


    void processarDigital();


public:

    // ========================================================
    // Construtor
    // ========================================================

    ArrayLinha(
        HardwareSerial& portaSerial,
        int RX,
        int TX,
        int velocidade = 115200
    );


    // ========================================================
    // Inicialização
    // ========================================================

    void begin();


    // ========================================================
    // Atualiza dados recebidos
    // ========================================================

    void update();


    // ========================================================
    // Retorna dados atuais
    // ========================================================

    ArrayData getData();

};

#endif